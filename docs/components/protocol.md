# Protocol

## Purpose
JSON messages used by the UI client and the headless core.

## Key responsibilities
- Define request and response shapes.
- Keep UI and core decoupled from transport details.

## Key locations
- `crates/protocol/src`.
- Prompt UI messages: `crates/protocol/src/prompt_ui.rs`.
